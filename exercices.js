//Return x doubled
function double (x) {
	return x * 2;
}

//x is a number, return x as a string
function numberToString(x) {
	return x + "";
}

//x is a float, return x as a truncated int
function floatToInt(x) {
	return Math.trunc(x);
}

//return x squared 
function square(x) {
	return x * x;
}
